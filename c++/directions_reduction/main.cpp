#include <bits/stdc++.h>

class DirReduction
{
    public:
        static std::vector<std::string> dirReduc(std::vector<std::string> &arr);
};

static std::vector<std::string> dirReduc(std::vector<std::string> &arr)
{
 for(unsigned i{0};i<)   

}

int main()
{
    return 0;
}